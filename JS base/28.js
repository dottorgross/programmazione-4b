/*  PROBLEMA
    dato un array persone contenente una serie di sottoarray del tipo ['Marisa','f']
    cioè per ogni nome è indicato anche il sesso
    creare un nuovo array potenzialiMorose che al suo interno abbia solo i
    nomi delle ragazze (solo i nomi, non anche di nuovo il sesso)
*/

    // SOLUZIONE
    // creo array persone con dentro le coppie [nome, sesso]
    let persone = [
        ['Marco', 'm'],
        ['Lorena', 'f'],
        ['Bianca', 'f'],
        ['Lillo', 'm']
    ];

    // creo array potenzialiMorose vuoto
    let potenzialiMorose = [];

    // ciclo for che passa in rassegna tutte le persone
    for(let i=0; i<persone.length; i++) {
    //     estraggo la singola persona (cioè persone[i])
    //     estraggo il nome
    //     estraggo il sesso
    //     se il sesso è 'f'
    //         aggiungo il nome all'array potenzialiMorose
    }

    // scrivo nella console potenzialiMorose
    console.log(potenzialiMorose);





